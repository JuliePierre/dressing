<div class="main-body">
  <div class="container">
    <h1>Mon panier</h1>
    <% @cart.shopping_cart_items.each do |element| %>
      <% item = VideDressingItem.find(element.item_id) %>
      <p><%= item.name %> - <%= item.category %></p>
    <% end %>
  </div>
</div>
