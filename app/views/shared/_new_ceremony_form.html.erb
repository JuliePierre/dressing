<%= simple_form_for(ceremony, remote: true) do |f| %>
  <%= f.error_notification %>

  <%= f.input :name, input_html: { placeholder: "Donnez-lui un nom" } %>

  <%= f.button :submit, class: 'highlight-btn' %>
<% end %>
